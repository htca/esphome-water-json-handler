
import esphome.codegen as cg

# Deze component heeft geen eigen YAML-opties; we gebruiken custom_component (lambda)
# Een minimale CONFIG_SCHEMA is voldoende om de module te laden.
CONFIG_SCHEMA = cg.Schema({})
``
